# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri sidecar binaries — fetched per host triple, not committed
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep

# Claude Code artifacts (skills, memory, settings, hooks) are all
# user-local — none of it lives in the repo. See CLAUDE.md.
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
